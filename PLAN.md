# Ambient Source Events 要件定義

## 1. 概要

OBS Studio上で任意の映像ソースに追加できるLuaベースの映像フィルタを開発する。

フィルタを適用したソースを、一定またはランダムな間隔で一時的に表示し、表示開始時・終了時にエフェクトを適用する。

主な用途は、配信画面などに対して、

- 動画
- アニメーション
- 静止画
- テキスト
- Browser Source
- その他の映像ソース

を一定間隔または不定期に出現させ、変化の少ない画面に自然な演出を加えることである。

仮称：

**Ambient Source Events**

日本語UI上では、英語が分からないユーザーにも機能が伝わる表示名を別途設定する。

候補：

- ソース定期表示
- ソース出現エフェクト
- ソース自動表示
- ソースイベント表示

内部名・スクリプト名とOBS上の表示名は分離してよい。

---

# 2. 基本構造

OBSの「ツール → スクリプト」からLuaスクリプトを登録する。

スクリプト登録後、対象ソースの

**フィルタ → ＋**

から本フィルタを追加できるようにする。

フィルタ自身が、対象ソースの表示タイミング・エフェクト・必要に応じたメディア再生を管理する。

---

# 3. 対象ソース

動画専用にはしない。

可能な限り任意の映像ソースで使用できる構造とする。

内部的には大きく以下の2種類に分けて扱う。

## 3.1 Media型

OBSのMedia Sourceなど、再生・終了という概念を持つソース。

例：

- 動画
- Media Sourceとして読み込んだGIF

Media型では、本フィルタがメディア再生を制御する。

基本的にOBS側のMedia Source設定は、

**Loop：OFF**

を前提とする。

イベント開始時にLua側からメディアを先頭から再スタートする。

動画が一度最後まで再生されて終了状態になっていても、次回イベント時に再スタートする。

---

## 3.2 Persistent型

明確な「再生終了」を持たないソース。

例：

- PNG
- JPEG
- Image Source
- Image Sourceとして読み込んだGIF
- Text
- Browser Source
- Capture系ソース
- その他の通常映像ソース

Persistent型では、フィルタ側で「表示時間」を管理する。

---

# 4. GIFの扱い

Animated GIFは読み込み方法によって扱いが変わる。

## Media Sourceとして読み込んだGIF

Media型として扱う。

イベントごとに再スタートできる。

## Image Sourceとして読み込んだGIF

Persistent型として扱う。

初版ではGIFそのものを強制的に先頭フレームへ戻す特殊処理は実装しない。

毎回アニメーションを最初から再生したい場合は、Media SourceとしてGIFを読み込むことを推奨する。

---

# 5. 状態管理

フィルタ内部では明確な状態を持つ。

基本状態：

```text
WAITING
↓
STARTING
↓
PLAYING / VISIBLE
↓
ENDING
↓
WAITING
```

Media型ではおおむね、

```text
WAITING
↓
Media Restart
↓
STARTING
↓
PLAYING
↓
ENDING
↓
Media End
↓
WAITING
```

Persistent型では、

```text
WAITING
↓
STARTING
↓
VISIBLE
↓
ENDING
↓
非表示
↓
WAITING
```

となる。

---

# 6. 再生・表示間隔

## 6.1 間隔モード

以下から選択可能とする。

- 固定間隔
- ランダム間隔

---

## 6.2 固定間隔

設定例：

```text
30.0 秒
```

小数入力を許可する。

例：

```text
0.5
12.75
30
120.5
```

---

## 6.3 ランダム間隔

最小値・最大値を設定する。

例：

```text
最小：20.0 秒
最大：60.0 秒
```

イベント終了後、次の待機時間を指定範囲からランダムに決定する。

小数値を許可する。

---

# 7. 間隔の基準

間隔は、

**前回のイベント終了後から、次回イベント開始まで**

とする。

例：

```text
動画：10秒
待機間隔：30秒
```

なら、

```text
0秒      再生開始
10秒     再生終了
10〜40秒 待機
40秒     次回再生
```

となる。

この仕様により、ソースの表示時間が待機時間より長い場合でも、再生中に次のイベントが発生する競合を避ける。

---

# 8. 初回イベント

フィルタが有効になった際の初回動作を選択できるようにする。

設定：

```text
初回表示

○ すぐに表示
○ 最初の待機時間を経過してから表示
```

ランダム間隔の場合、「待機してから」を選択すると、初回もランダム範囲から待機時間を生成する。

---

# 9. 開始エフェクト

初版では以下を実装する。

```text
開始エフェクト

○ なし
○ フェード
```

開始エフェクトの時間を設定できる。

例：

```text
0.5 秒
```

小数入力可。

---

# 10. 終了エフェクト

開始エフェクトとは独立して設定する。

```text
終了エフェクト

○ なし
○ フェード
```

時間：

```text
0.5 秒
```

小数入力可。

例えば、

```text
開始：フェード
終了：なし
```

あるいは、

```text
開始：なし
終了：フェード
```

も許可する。

---

# 11. フェード仕様

フェードイン：

```text
透明度 0%
↓
透明度 100%
```

フェードアウト：

```text
透明度 100%
↓
透明度 0%
```

フレーム更新に合わせて滑らかに補間する。

初版では基本的な補間方式でよい。

内部構造は将来的に、

- Linear
- Ease In
- Ease Out
- Ease In-Out

などを追加できるようにしておく。

初版ではこれらの補間方式をUIに露出させなくてもよい。

---

# 12. Persistent型の表示時間

Persistent型には、

```text
表示時間
```

を設定する。

例：

```text
5.0 秒
```

小数入力可能。

---

# 13. 表示時間の数え方

Persistent型では、エフェクト時間を表示時間に含めるか選べるようにする。

UI表示：

```text
表示時間の数え方

○ 表示時間の中に開始・終了エフェクトを含める
○ 表示時間とは別に開始・終了エフェクトを追加する
```

デフォルト：

**表示時間の中に開始・終了エフェクトを含める**

とする。

---

## 13.1 エフェクト時間を含める場合

設定：

```text
表示時間：5.0秒
開始フェード：0.5秒
終了フェード：0.5秒
```

動作：

```text
0.5秒 フェードイン
4.0秒 完全表示
0.5秒 フェードアウト

合計：5.0秒
```

---

## 13.2 エフェクト時間を含めない場合

同じ設定なら、

```text
0.5秒 フェードイン
5.0秒 完全表示
0.5秒 フェードアウト

合計：6.0秒
```

となる。

---

# 14. Media型の終了処理

Media型では動画などの再生終了を利用する。

可能であれば、

- メディア現在位置
- メディア長
- メディア状態
- Media Endイベント

を組み合わせて終了を検知する。

終了フェードを使用する場合は、動画終了直前からフェードアウトを開始する。

例：

```text
動画長：10秒
終了フェード：0.5秒
```

の場合、

```text
9.5秒
```

付近からフェードアウトを開始する。

メディアの実終了も別途検知し、状態不整合を防止する。

---

# 15. 「終了エフェクトなし」の意味

Persistent型では、終了エフェクトを「なし」にしてもイベント終了後は非表示にする必要がある。

したがって、

```text
終了エフェクト：なし
```

は、

```text
透明度100%
↓
即座に0%
```

という意味とする。

イベント終了後は必ず非表示状態に戻す。

Media型の場合も、次回イベントまで不要な映像が残らないようにする。

---

# 16. フィルタ無効化時

フィルタを無効にした場合は、フィルタによる表示状態を解除する。

基本動作：

```text
透明度 = 100%
```

に戻す。

フィルタを無効にした結果、

「ソースが透明なまま戻らない」

という状態を発生させない。

---

# 17. ソースが非アクティブな場合

初版では、

**対象ソースが非アクティブな間はタイマーを停止する**

仕様とする。

例：

```text
Scene A
対象ソースあり
↓
タイマー進行

Scene Bへ移動
↓
タイマー停止

Scene Aへ戻る
↓
残り時間から再開
```

ユーザーが実際に表示していないシーンにいる間にイベントを消費しないためである。

将来的には、

```text
○ 非アクティブ中は一時停止
○ 非アクティブ中もタイマーを進める
```

という設定追加を検討してよい。

初版は一時停止固定。

---

# 18. Media Sourceの所有権

本フィルタを利用するMedia Sourceでは、本フィルタが再生タイミングを管理するものとする。

基本的に以下との併用は想定しない。

- OBS Media SourceのLoop
- 他スクリプトによる自動Restart
- 同一ソースを別機構から周期的に再生する処理

本フィルタ使用時はLoop OFFを推奨する。

この前提により、状態管理を単純かつ安定させる。

---

# 19. 初版UI案

```text
Ambient Source Events
────────────────────────

【タイミング】

間隔モード
[ 固定 / ランダム ]

固定間隔
[ 30.0 ] 秒

ランダム最小
[ 20.0 ] 秒

ランダム最大
[ 60.0 ] 秒


【初回表示】

[ すぐに表示 ]
[ 最初の待機時間を経過してから表示 ]


【表示】

表示時間
[ 5.0 ] 秒

表示時間の数え方
[ 表示時間の中に開始・終了エフェクトを含める ]
[ 表示時間とは別に開始・終了エフェクトを追加する ]


【開始エフェクト】

種類
[ なし / フェード ]

時間
[ 0.5 ] 秒


【終了エフェクト】

種類
[ なし / フェード ]

時間
[ 0.5 ] 秒
```

Media型では不要な「表示時間」関連項目を非表示または無効化できるなら、その方が望ましい。

---

# 20. 数値入力

秒数を扱うすべての設定は小数を許可する。

対象：

- 固定間隔
- ランダム最小
- ランダム最大
- 表示時間
- 開始エフェクト時間
- 終了エフェクト時間

例：

```text
0.1
0.25
1.5
12.75
30.0
```

不正値や負数を受け付けないよう適切な下限を設定する。

---

# 21. 設定値の整合性

最低限、以下を処理する。

ランダム間隔：

```text
最小値 > 最大値
```

となった場合に破綻しないこと。

開始・終了エフェクト時間が、表示時間を超える場合も破綻しないこと。

特に、

```text
表示時間の中にエフェクトを含める
```

モードで、

```text
開始時間 + 終了時間 > 表示時間
```

となった場合の動作を安全に処理する。

初版では、値を自動的にクランプするか、完全表示時間を0として処理する方法でよい。

クラッシュや負の時間計算を発生させないことを優先する。

---

# 22. 将来追加予定のエフェクト

初版では実装しないが、追加可能な設計にしておく。

候補：

- Slide
- Zoom
- Pop
- Wipe
- Rotate
- Pulse

開始・終了で別々のエフェクトを選択できる構造を維持する。

例：

```text
開始：Zoom
終了：Fade
```

などを将来的に可能にする。

---

# 23. 将来追加を検討できる機能

初版には含めない。

- フェード補間方式選択
- 非アクティブ中もタイマー継続
- ランダム表示時間
- エフェクトごとの詳細設定
- 複数エフェクトの組み合わせ
- イベント発生確率
- 一定回数ごとのイベント
- 初回のみ異なる待機時間
- Media Source以外のAnimated GIF再スタート制御
- テスト再生ボタン
- 次回イベントまでの残り時間表示

---

# 24. 開発上の優先順位

初版では機能を増やしすぎず、以下を最優先する。

1. OBSで映像フィルタとして正常に登録できる
2. Persistent型ソースを一定間隔で表示・非表示できる
3. Media Sourceをイベントごとに先頭から再生できる
4. 固定間隔
5. ランダム間隔
6. 小数秒入力
7. 開始フェード
8. 終了フェード
9. 初回即時／待機
10. ソース非アクティブ時のタイマー停止
11. フィルタ無効化時の正常復帰
12. OBS終了・シーン切替・フィルタ削除などでクラッシュしない

---

# 25. 初版の完成条件

以下を満たせば初版完成とする。

### 静止画

PNGにフィルタを追加し、

```text
30秒待機
↓
0.5秒フェードイン
↓
5秒表示
↓
0.5秒フェードアウト
↓
30秒待機
```

を繰り返せる。

### ランダム

```text
20〜60秒
```

の範囲でイベント間隔が毎回変化する。

### 動画

Media Sourceにフィルタを追加し、

```text
待機
↓
動画を先頭から再生
↓
開始フェード
↓
動画再生
↓
終了フェード
↓
動画終了
↓
次の待機
```

を繰り返せる。

Media SourceのLoopはOFFのままで動作する。

### シーン切替

対象ソースが非アクティブになった場合はタイマーが停止し、戻った際に正常に再開する。

### フィルタ無効化

フィルタを無効にすると、対象ソースは通常の表示状態へ戻る。

---

# 26. 設計思想

本スクリプトは単なる動画再生タイマーではなく、

**「OBS上の任意のソースを、一時的なイベントとして出現させる仕組み」**

として設計する。

例えば複数のソースへそれぞれ、

```text
葉っぱ
30〜60秒

光
20〜45秒

キャラクター
120〜300秒

キラキラ
45〜90秒
```

のような設定を行うことで、通常は静止している配信画面上に、不規則に小さな出来事が起きる環境を構築できる。

この用途を初版以降の機能拡張でも維持する。